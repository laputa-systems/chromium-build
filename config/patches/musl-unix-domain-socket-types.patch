diff --git a/base/posix/unix_domain_socket.cc b/base/posix/unix_domain_socket.cc
--- a/base/posix/unix_domain_socket.cc
+++ b/base/posix/unix_domain_socket.cc
@@ -76,1 +76,1 @@
-    msg.msg_controllen = control_len;
+    msg.msg_controllen = static_cast<decltype(msg.msg_controllen)>(control_len);
@@ -84,1 +84,1 @@
-    cmsg->cmsg_len = CMSG_LEN(sizeof(int) * fds.size());
+    cmsg->cmsg_len = static_cast<decltype(cmsg->cmsg_len)>(CMSG_LEN(sizeof(int) * fds.size()));
@@ -149,0 +150,4 @@
+#if defined(__linux__) && !defined(__GLIBC__)
+#pragma GCC diagnostic push
+#pragma GCC diagnostic ignored "-Wsign-compare"
+#endif
@@ -171,0 +176,3 @@
+#if defined(__linux__) && !defined(__GLIBC__)
+#pragma GCC diagnostic pop
+#endif
